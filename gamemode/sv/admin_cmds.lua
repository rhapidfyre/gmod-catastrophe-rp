
-- Set player's team
local function SetTeam(ply, number)
end

-- Reset (player)'s skill tree
-- bool: Give points back to spend again
local function ResetTree(ply, points)
end

-- Force (player) to re-enter character information
-- bool: If true, lets the player close it with no changes
local function CharSetup(ply, exit)
end

-- Give (player) X amount of cash
local function GiveCash(ply, value)
end

-- Slay given mob number. If no number given, return table of living NPCs
local function SlayMob(num)
end

-- Allow admin to use noclip
local function Noclip(admin)
end

-- Allow admin to not die
-- Note: Admins won't be able to kill anyone either
local function BestFriend(admin)
end